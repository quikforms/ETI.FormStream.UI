# ngx-bootstrap 7.0.1 declares a stale peer range (@angular/* ^11 || ^12) but is
# compatible with the Angular 15 toolchain this project uses. Installing with the
# legacy peer-dependency algorithm avoids a false-positive ERESOLVE failure while
# keeping installs reproducible across machines and CI.
legacy-peer-deps=true
